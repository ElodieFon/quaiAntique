<header>
    <div class="p-4">
        <h2 class="text-center">{{titlePage}}</h2>
        <h3 class="text-center">{{subtitle}}</h3>
        
        <div class="d-flex flex-row-reverse col-12 btn-reservation">
            {% if app.request.attributes.get('_route') != 'app_reservation' %}
                <a href="{{ path('app_reservation') }}" class="btn btn-success">Reserver</a>
            {% endif %}
        </div>
    </div>
</header>