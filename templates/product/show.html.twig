{% extends 'base.html.twig' %}

{% block stylesheet %}
    {{ parent() }}
    
{% endblock %}
{% block title %}carte / {{product.name}}{% endblock %}

{% block body %}
    <main>
        {% include "base/header.html.twig" %} 

           
            <div class="d-flex p-2 m-2">
                <div class="col-4 m-auto block-img">
                    <img src="/images/products/{{product.image}}" alt="{{product.name}}" style="width: 100%;"/>
                </div>
                <div class="d-flex align-items-center justify-content-center flex-column col-4 m-auto">
                    <h2 class="mb-3 text-capitalize">{{product.name}}</h2>
                    <p> 
                        {{product.description}}
                    </p>
                    <span class="text-danger fs-2">{{product.prix / 100}} €</span>
                </div>
                
            </div>
           
    </main>

{% endblock %}
